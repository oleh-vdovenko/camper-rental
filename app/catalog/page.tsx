import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Catalog',
  description: 'Catalog of campers available for rent',
};

const Catalog = () => {
  return <div>Catalog</div>;
};
export default Catalog;
